import React from "react";
import {
  ChakraProvider,
  Grid,
  GridItem,
  Box,
  Text,
  Stack,
} from "@chakra-ui/react";

const Timetable = () => {
  // 시간표 데이터

  return (
    <Stack>
      <Text>TIMETABLE</Text>
    </Stack>
  );
};

export default Timetable;
